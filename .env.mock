# Carregado automaticamente quando o Vite roda em modo "mock"
# (npm run dev:mock  ->  vite --mode mock).
# Aponta o FE para o mock BE local (json-server em mock/server.cjs).
VITE_API_URL=http://localhost:3001
